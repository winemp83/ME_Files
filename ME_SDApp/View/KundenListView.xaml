<UserControl x:Class="View.KundenListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Kunden}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kunden Nummer" Binding="{Binding KundenNummer}" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Debug" Binding="{Binding Model}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Kunden Hinzufügen" Command="{Binding AddKundenCommand}" Grid.Row="1" />
    </Grid>
</UserControl>
